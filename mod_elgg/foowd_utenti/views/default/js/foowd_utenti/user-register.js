// Generated by CoffeeScript 1.9.3
(function(root, factory) {
  if (typeof define === 'function' && define.amd) {
    return define(['elgg', 'jquery', 'handlebars', 'crop'], factory);
  } else if (typeof exports === 'object') {
    return module.exports = factory();
  } else {
    return root.returnExports = factory();
  }
})(this, function() {
  var $, Jform, Jgenre, Jhook, crop, elgg, init;
  elgg = require('elgg');
  $ = require('jquery');
  crop = require('crop');
  Jhook = $('#offer-hook');
  Jform = Jhook.parents('form:first');
  Jgenre = $('[name=Genre]');
  Jform.attr('enctype', 'multipart/form-data');
  Jhook.css({
    display: 'none'
  });
  Jgenre.val('standard');
  Jgenre.on("change", function() {
    if ($(this).val() === 'offerente') {
      return Jhook.fadeIn('slow');
    } else {
      return Jhook.fadeOut('slow');
    }
  });
  init = {
    urlF: document.getElementById('url').href,
    loadedImageContainerId: 'image',
    sourceId: 'sorgente',
    fileId: 'loadedFile',
    imageContainerId: 'image-container',
    css: ['mod/foowd_utility/js/imgareaselect/css/imgareaselect-default.css', 'mod/foowd_utility/js/foowd-crop/foowd-crop.css']
  };
  return crop.initialize(init);
});
